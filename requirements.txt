# --- Infrastructure & Sandbox ---
watchdog>=3.0.0          # For prober.py to monitor file changes
python-dotenv>=1.0.0     # To load OPENAI_API_KEY from .env

# --- AI & Agent Capabilities (Builder/Architect) ---
openai>=1.12.0           # Main LLM Client (GPT-4)
tiktoken>=0.5.0          # Tokenizer for accurate context window management

# --- Memory & RAG (VectorStore) ---
chromadb>=0.4.22         # Lightweight local vector database for your "Memories"
# faiss-cpu              # Alternative if you prefer Facebook AI Similarity Search

# --- Data & Analytics (RL Logging) ---
pandas>=2.0.0            # To analyze the .jsonl logs generated by the loop
numpy>=1.24.0            # For reward calculations and math operations
